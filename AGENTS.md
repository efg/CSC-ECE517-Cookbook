# Cookbook Application

This is a rails 8 application that lives inside of a docker container.

Docker files are reference under BuildTools

The cookbook application lives under the cookbook directory

We use Node.JS as our javascript library for rails.

